/** @type {import('next').NextConfig} */

const nextConfig = {
    eslint: {
        ignoreDuringBuilds: true,
    },
        // images: {
          
        //   formats:['image/webp']
        // },
/* ...Your other config rules */
}

module.exports = nextConfig